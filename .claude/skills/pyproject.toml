[project]
name = "claude-skills"
version = "0.1.0"
description = "Shared Python dependencies for Claude Code skills"
requires-python = ">=3.11"
dependencies = [
    "PyMuPDF>=1.24",
    "requests>=2.31",
]
